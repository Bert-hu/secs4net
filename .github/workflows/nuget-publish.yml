# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "base" ]
  pull_request:
    branches: [ "base" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore
    # - name: Test
    #   run: dotnet test --no-build --verbosity normal
    - name: Pack Secs4Net.Bert
      run: dotnet pack src/Secs4Net/Secs4Net.Bert.csproj -c Release --no-build
      working-directory: ./src/Secs4Net
    - name: Push Secs4Net.Bert
      run: dotnet nuget push src/Secs4Net/bin/Release/*.nupkg --skip-duplicate -k ${{ secrets.NUGET_API_KEY }} -s https://api.nuget.org/v3/index.json
      working-directory: ./src/Secs4Net
    - name: Pack Secs4Net.Sml.Bert
      run: dotnet pack src/Secs4Net.Sml/Secs4Net.Sml.Bert.csproj -c Release --no-build
      working-directory: ./src/Secs4Net.Sml
    - name: Push Secs4Net.Sml.Bert
      run: dotnet nuget push src/Secs4Net.Sml/bin/Release/*.nupkg --skip-duplicate -k ${{ secrets.NUGET_API_KEY }} -s https://api.nuget.org/v3/index.json
      working-directory: ./src/Secs4Net.Sml